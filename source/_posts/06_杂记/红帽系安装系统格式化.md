# 红帽系安装系统格式化硬盘

>解决的问题：安装系统时，硬盘上面残留有原系统数据，导致所选盘无法完全格式化或者系统安装失败。

解决方法：

1、使用系统盘引导，进入OS的**图形化**安装的界面

2、CTRL + ALT + F3 进到**命令行**界面

3、使用命令清空硬盘

```shell
# 清除硬盘分区标签
wipefs -a /dev/sda
# 清除硬盘前10MB（包括MBR、GPT表）
dd if=/dev/zero of=/dev/sda bs=1M count=10
```
